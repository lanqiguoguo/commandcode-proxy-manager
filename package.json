{
  "name": "commandcode-proxy-manager",
  "version": "0.1.0",
  "description": "Multi-key management gateway for commandcode-proxy: active/standby key pool, 429 backoff, quota dashboard, zero-build admin SPA. Bundles the upstream proxy as-is.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0 <21.0.0"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "dev": "node --watch src/server.mjs",
    "test": "node scripts/unit.mjs && node scripts/e2e.mjs && node scripts/test-server-lifecycle.mjs && bash scripts/test-sync-upstream.sh",
    "test:unit": "node scripts/unit.mjs",
    "test:e2e": "node scripts/e2e.mjs",
    "test:lifecycle": "node scripts/test-server-lifecycle.mjs",
    "sync:upstream": "bash scripts/sync-upstream.sh",
    "docker:build": "docker build -t commandcode-proxy-manager:latest .",
    "docker:build:multi": "docker buildx build --platform linux/amd64,linux/arm64 -t commandcode-proxy-manager:latest ."
  }
}
